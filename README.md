# strong-idempotent-payments-api
Production-grade Payments API with X-Idempotency-Key + request hash, Postgres advisory-lock dedup (anti double-charge), outbox -> Kafka publisher with retries/DLQ, Redis cache, Flyway, full integration tests, and Postman collection
